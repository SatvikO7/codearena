# Frontend build-time configuration.
# Vite exposes only variables prefixed with VITE_ to the browser bundle, so no
# server-side secret can be leaked through this file.
VITE_API_BASE_URL=http://localhost:8080
